{
  "name": "esports-india",
  "private": true,
  "workspaces": [
    "tournament-website"
  ],
  "scripts": {
    "vercel-build": "npm --workspace tournament-website run build",
    "build": "npm run vercel-build",
    "dev": "npm --workspace tournament-website run dev",
    "start": "npm --workspace tournament-website run start",
    "lint": "npm --workspace tournament-website run lint"
  },
  "devDependencies": {
    "next": "15.2.4"
  }
}
