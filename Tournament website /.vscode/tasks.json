{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Next.js production build (clean)",
			"type": "shell",
			"command": "rm -rf .next && npm run build",
			"problemMatcher": [
				"$tsc"
			],
			"group": "build"
		}
	]
}